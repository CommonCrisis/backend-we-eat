# from fastapi import APIRouter
# from fastapi import BackgroundTasks
# router = APIRouter()
# @router.get('/session/')
# async def create_session(background_tasks: BackgroundTasks):
#     background_tasks.add_task(func, param_1, param_2='some notification')
#     return {'info': 'session created', 'sessionId': 1234}
